import { TranslateBoolPipe } from './translate-bool.pipe';

describe('TranslateBoolPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateBoolPipe();
    expect(pipe).toBeTruthy();
  });
});
